---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: bosh/integration
    tag: main

inputs:
  - name: bosh-cpi-src
  - name: bosh-deployment
  - name: environment

outputs:
  - name: bosh-deployment

run:
  path: bosh-cpi-src/ci/tasks/patch-director-role.sh

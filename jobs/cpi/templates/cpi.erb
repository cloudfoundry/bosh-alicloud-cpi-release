#!/bin/bash

read INPUT

BOSH_PACKAGES_DIR=${BOSH_PACKAGES_DIR:-/var/vcap/packages}
BOSH_JOBS_DIR=${BOSH_JOBS_DIR:-/var/vcap/jobs}

PATH=$BOSH_PACKAGES_DIR/ruby_aliyun_cpi/bin:$PATH
export PATH

export BUNDLE_GEMFILE=$BOSH_PACKAGES_DIR/bosh_aliyun_cpi/Gemfile

bundle_cmd="$BOSH_PACKAGES_DIR/ruby_aliyun_cpi/bin/bundle"

echo $INPUT | $bundle_cmd exec $BOSH_PACKAGES_DIR/bosh_aliyun_cpi/bin/aliyun_cpi \
  $BOSH_JOBS_DIR/cpi/config/cpi.json

exit 0
